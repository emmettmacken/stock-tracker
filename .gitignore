.idea/
.DS_Store
SYSTEM_REVIEW.md

# Installed agent skill packages (tooling, not project code)
.agents/
.claude/skills
skills-lock.json

# Stray SQLite DB created when run from repo root (real DB lives in backend/)
stock_tracker.db
stock_tracker.db-shm
stock_tracker.db-wal
